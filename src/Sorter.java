public interface Sorter {
    // abstract all sorter classes

    // abstract methods

    //prints the correct formatting and with the name of the sort method
    void printSortName();

    // prints the original array before it is sorted
    void arrayBefore();

    // prints out the sorted array
    void arraySorted();

}